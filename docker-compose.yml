version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: african-art-exhibition
    ports:
      - "8080:80"
    volumes:
      - ./:/usr/share/nginx/html:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
    restart: unless-stopped
